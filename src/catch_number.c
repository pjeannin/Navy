/*
** EPITECH PROJECT, 2020
** catch_number.c
** File description:
** catch_number
*/

#include "navy.h"

int catch_number(void)
{
    int count_sig = 0;

    sig = 0;
        while (sig < 12) {
            count_sig = sig;
            pause();
        }
    return (count_sig);
}